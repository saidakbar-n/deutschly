FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY bot ./bot

CMD ["python", "-c", "import os, subprocess; port=os.getenv('PORT','8080'); subprocess.run(['uvicorn','bot.main:api_app','--host','0.0.0.0','--port',port])"]
